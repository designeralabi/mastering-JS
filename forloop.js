// and array of schoolnames
var schoolnames = ['unilag', 'laspotech', 'OAU', 'tasued'];


// using for loop to iterate through schoolnames
for (let index = 0; index < schoolnames.length; index++) {

    // return the name of school
    const element = schoolnames[index];
    //prints our the result
    console.log(index, element);
    
}